# OilTrace backend (FastAPI).
# Live Axios client tries PRIMARY then failovers to FALLBACK (see src/services/api.js).

VITE_BACKEND_PRIMARY_URL=https://vscimatic999--oiltrace-backend-web.modal.run
VITE_BACKEND_FALLBACK_URL=https://sih-oil-spill-26143-backend.onrender.com

# Direct ML service (no CORS). Dev uses Vite /ml-api proxy.
# VITE_ML_BASE_URL=/ml-api
